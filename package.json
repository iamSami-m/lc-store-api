{
  "name": "lc-store-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch data/db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}